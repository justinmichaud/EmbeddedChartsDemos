# esbuild's optional platform binary is present and functional; skip the
# pre-run dependency verification that otherwise fails `pnpm dev` on the
# (harmless) ignored-build-scripts warning.
verify-deps-before-run=false
